{
  "name": "dates-examples",
  "version": "0.0.1",
  "description": "This package tests all most known date packages being used in same use cases",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Afonso Araújo Neto",
  "license": "ISC",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
